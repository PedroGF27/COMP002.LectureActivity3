function greet(name) {
    console.log('Hello, ' + name);
}
(greet("Pedro"));

function circumference(diameter) {
    const pi = Math.PI;
    return pi * diameter;
}

console.log(circumference(12));

